<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoSynergy"
        tools:targetApi="31">

        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity android:name=".LoginActivity" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- LearningActivity -->
        <activity android:name=".LearningActivity" />

        <!-- CollaborationActivity -->
        <activity android:name=".CollaborationActivity" />

        <!-- Additional collaboration-related activities -->
        <activity android:name=".CollabProjectsActivity" />
        <activity android:name=".CollabDiscussActivity" />
        <activity android:name=".CollabPartnersActivity" />
        <activity android:name=".CollabProjectsAddActivity" />
        <activity android:name=".CollabProjectsDescActivity" />

        <!-- DashboardActivity -->
        <activity android:name=".DashboardActivity" />

        <!-- AdminActivity -->
        <activity android:name=".AdminActivity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!-- NewsActivity -->
        <activity android:name=".NewsActivity" />

        <!-- NewsDetailsActivity -->
        <activity android:name=".NewsDetailsActivity" />

        <!-- Firebase Configuration -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primaryColor" />

    </application>

</manifest>
